<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard - Student Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        function setTheme(theme) {
            const body = document.body;
            body.classList.remove('theme-glass', 'theme-dark');
            body.classList.add(theme);
            localStorage.setItem('sms_theme', theme); // Using a specific key for our app
            
            // Update active button state
            document.querySelectorAll('.theme-btn').forEach(btn => {
                btn.classList.remove('active');
                if (btn.dataset.theme === theme) {
                    btn.classList.add('active');
                }
            });
        }

        // Listen for theme changes from other pages
        window.addEventListener('storage', (e) => {
            if (e.key === 'sms_theme') {
                setTheme(e.newValue);
            }
        });

        document.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('sms_theme') || 'theme-glass';
            setTheme(savedTheme);
        });
    </script>
    <style>
        /* Glass Theme (Current) */
        .theme-glass {
            background: linear-gradient(135deg, rgb(15, 23, 42) 0%, rgb(88, 28, 135) 50%, rgb(15, 23, 42) 100%);
            min-height: 100vh;
        }
        .theme-glass .header-bg {
            background: rgba(15, 23, 42, 0.8);
            backdrop-filter: blur(16px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        .theme-glass .card-bg {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .theme-glass .text-theme-primary {
            color: white;
        }
        .theme-glass .text-theme-secondary {
            color: rgba(255, 255, 255, 0.7);
        }

        /* Dark Professional Theme */
        .theme-dark {
            background-color: #0f172a;
            min-height: 100vh;
        }
        .theme-dark .header-bg {
            background-color: #1e293b;
            border-bottom: 1px solid #334155;
        }
        .theme-dark .card-bg {
            background-color: #1e293b;
            border: 1px solid #334155;
        }
        .theme-dark .text-theme-primary {
            color: #e2e8f0;
        }
        .theme-dark .text-theme-secondary {
            color: #94a3b8;
        }
        .theme-dark .button-primary {
            background: linear-gradient(135deg, #1e40af 0%, #3730a3 100%);
        }
        .theme-dark .table-row-alt {
            background-color: #1e293b;
        }
        .theme-dark .table-border {
            border-color: #334155;
        }

        

        /* Theme Toggle */
        .theme-toggle {
            position: fixed;
            top: 1rem;
            right: 1rem;
            display: flex;
            gap: 0.5rem;
            background: rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(8px);
            padding: 0.25rem;
            border-radius: 9999px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            z-index: 60;
        }
        .theme-btn {
            width: 2rem;
            height: 2rem;
            border-radius: 9999px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: rgba(255, 255, 255, 0.8);
            transition: all 0.2s;
            cursor: pointer;
        }
        .theme-btn:hover {
            background: rgba(255, 255, 255, 0.1);
        }
        .theme-btn.active {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }
    </style>
</head>
<body class="min-h-screen">
    <!-- Theme Toggle Buttons -->
    <div class="theme-toggle" style="right: auto; left: 1rem;">
        <button class="theme-btn" data-theme="theme-glass" onclick="setTheme('theme-glass')" title="Glass Theme">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
            </svg>
        </button>
        <button class="theme-btn" data-theme="theme-dark" onclick="setTheme('theme-dark')" title="Dark Theme">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
            </svg>
        </button>
    </div>
    <!-- Navigation Header -->
    <header class="sticky top-0 z-50 header-bg shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <!-- Logo & Title -->
                <div class="flex items-center space-x-3">
                    <div class="flex items-center justify-center w-10 h-10 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl shadow-lg">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                        </svg>
                    </div>
                    <div>
                        <h1 class="text-lg font-bold text-theme-primary">Student Management</h1>
                        <p class="text-xs text-theme-secondary">Shipsy Assignment</p>
                    </div>
                </div>

                <!-- User Info & Actions -->
                <div class="flex items-center space-x-4">
                    <div class="hidden sm:block text-right">
                        <p class="text-sm font-medium text-theme-primary">Admin User</p>
                        <p class="text-xs text-theme-secondary">System Administrator</p>
                    </div>
                    <form action="/auth/logout" method="POST" class="inline">
                        <button type="submit" class="flex items-center px-4 py-2 text-sm font-medium text-theme-secondary hover:text-theme-primary bg-white/5 hover:bg-white/10 rounded-lg transition-all duration-200 border border-white/10">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                            </svg>
                            Logout
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <!-- Page Header -->
        <div class="mb-8 card-bg rounded-xl p-6">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h2 class="text-3xl font-bold text-white mb-2">Student Dashboard</h2>
                    <p class="text-gray-400">Manage and monitor all student records</p>
                </div>
                <a href="/students/add" class="inline-flex items-center justify-center px-6 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white font-semibold rounded-xl shadow-lg transform transition-all duration-200 hover:scale-105">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                    </svg>
                    Add New Student
                </a>
            </div>
        </div>

        <!-- Filters Section -->
        <div class="mb-6 bg-white/5 backdrop-blur-xl rounded-2xl border border-white/10 p-6 shadow-xl">
            <form action="/students" method="GET" class="space-y-4">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                    <!-- Search by Name -->
                    <div>
                        <label class="block text-sm font-semibold text-gray-200 mb-2">
                            <svg class="inline w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                            </svg>
                            Search by Name
                        </label>
                        <input 
                            type="text" 
                            name="name" 
                            placeholder="Enter student name..." 
                            class="w-full px-4 py-2.5 bg-white/5 border border-white/10 rounded-xl text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition-all"
                            value="<%= locals.filters && filters.name ? filters.name : '' %>"
                        >
                    </div>

                    <!-- Filter by Branch -->
                    <div>
                        <label class="block text-sm font-semibold text-gray-200 mb-2">
                            <svg class="inline w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"></path>
                            </svg>
                            Filter by Branch
                        </label>
                        <select 
                            name="branch" 
                            class="w-full px-4 py-2.5 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:ring-2 focus:ring-indigo-500 transition-all appearance-none [&>option]:bg-neutral-800 [&>option]:text-white"
                        >
                            <option value="" class="bg-neutral-800 text-white">All Branches</option>
                            <% const branches = ['CSE', 'IT', 'ECE', 'ME', 'CE']; %>
                            <% branches.forEach(branch => { %>
                                <option value="<%= branch %>" <%= (locals.filters && filters.branch === branch) ? 'selected' : '' %> class="bg-neutral-800 text-white"><%= branch %></option>
                            <% }); %>
                        </select>
                    </div>

                    <!-- Apply Button -->
                    <div class="flex items-end">
                        <button 
                            type="submit" 
                            class="w-full px-6 py-2.5 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-xl shadow-lg transform transition-all duration-200 hover:scale-105"
                        >
                            Apply Filters
                        </button>
                    </div>
                </div>

                <!-- Active Filters Display -->
                <% if ((locals.filters && filters.name) || (locals.filters && filters.branch)) { %>
                    <div class="flex flex-wrap items-center gap-2 pt-4 border-t border-white/10">
                        <span class="text-sm text-gray-400">Active filters:</span>
                        <% if (filters.name) { %>
                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-indigo-500/20 text-indigo-300 border border-indigo-500/30">
                                Name: <%= filters.name %>
                                <a href="/students?branch=<%= filters.branch || '' %>" class="ml-2 hover:text-indigo-200">×</a>
                            </span>
                        <% } %>
                        <% if (filters.branch) { %>
                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-purple-500/20 text-purple-300 border border-purple-500/30">
                                Branch: <%= filters.branch %>
                                <a href="/students?name=<%= filters.name || '' %>" class="ml-2 hover:text-purple-200">×</a>
                            </span>
                        <% } %>
                        <a href="/students" class="text-xs text-gray-400 hover:text-white ml-2">Clear all</a>
                    </div>
                <% } %>
            </form>
        </div>

        <!-- Students Table -->
        <div class="bg-white/5 backdrop-blur-xl rounded-2xl border border-white/10 shadow-xl overflow-hidden">
            <!-- Table Header Info -->
            <div class="px-6 py-4 border-b border-white/10">
                <div class="flex items-center justify-between">
                    <h3 class="text-lg font-semibold text-white">Student Records</h3>
                    <span class="px-3 py-1 text-sm font-medium text-indigo-300 bg-indigo-500/20 rounded-full border border-indigo-500/30">
                        <%= students.length %> students found
                    </span>
                </div>
            </div>

            <!-- Responsive Table -->
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-white/10">
                    <thead class="bg-white/5">
                        <tr>
                            <th scope="col" class="px-6 py-4 text-left">
                                <a href="?sortBy=name&branch=<%= filters.branch || '' %>&name=<%= filters.name || '' %>" class="flex items-center text-xs font-semibold text-gray-300 uppercase tracking-wider hover:text-white transition-colors group">
                                    Name
                                    <svg class="w-4 h-4 ml-1 opacity-0 group-hover:opacity-100 transition-opacity" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16V4m0 0L3 8m4-4l4 4m6 0v12m0 0l4-4m-4 4l-4-4"></path>
                                    </svg>
                                </a>
                            </th>
                            <th scope="col" class="px-6 py-4 text-left">
                                <a href="?sortBy=branch&branch=<%= filters.branch || '' %>&name=<%= filters.name || '' %>" class="flex items-center text-xs font-semibold text-gray-300 uppercase tracking-wider hover:text-white transition-colors group">
                                    Branch
                                    <svg class="w-4 h-4 ml-1 opacity-0 group-hover:opacity-100 transition-opacity" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16V4m0 0L3 8m4-4l4 4m6 0v12m0 0l4-4m-4 4l-4-4"></path>
                                    </svg>
                                </a>
                            </th>
                            <th scope="col" class="px-6 py-4 text-left text-xs font-semibold text-gray-300 uppercase tracking-wider">
                                Status
                            </th>
                            <th scope="col" class="px-6 py-4 text-left">
                                <a href="?sortBy=-cgpa&branch=<%= filters.branch || '' %>&name=<%= filters.name || '' %>" class="flex items-center text-xs font-semibold text-gray-300 uppercase tracking-wider hover:text-white transition-colors group">
                                    CGPA
                                    <svg class="w-4 h-4 ml-1 opacity-0 group-hover:opacity-100 transition-opacity" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16V4m0 0L3 8m4-4l4 4m6 0v12m0 0l4-4m-4 4l-4-4"></path>
                                    </svg>
                                </a>
                            </th>
                            <th scope="col" class="px-6 py-4 text-left text-xs font-semibold text-gray-300 uppercase tracking-wider">
                                Attendance
                            </th>
                            <th scope="col" class="px-6 py-4 text-left">
                                <a href="?sortBy=-overallPercentage&branch=<%= filters.branch || '' %>&name=<%= filters.name || '' %>" class="flex items-center text-xs font-semibold text-gray-300 uppercase tracking-wider hover:text-white transition-colors group">
                                    Overall %
                                    <svg class="w-4 h-4 ml-1 opacity-0 group-hover:opacity-100 transition-opacity" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16V4m0 0L3 8m4-4l4 4m6 0v12m0 0l4-4m-4 4l-4-4"></path>
                                    </svg>
                                </a>
                            </th>
                            <th scope="col" class="px-6 py-4 text-right text-xs font-semibold text-gray-300 uppercase tracking-wider">
                                Actions
                            </th>
                        </tr>
                    </thead>
                    <tbody class="divide-y table-border">
                        <% if (students.length === 0) { %>
                            <tr>
                                <td colspan="7" class="px-6 py-12 text-center">
                                    <div class="flex flex-col items-center justify-center">
                                        <svg class="w-16 h-16 text-theme-secondary mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
                                        </svg>
                                        <p class="text-theme-primary text-lg font-medium">No students found</p>
                                        <p class="text-theme-secondary text-sm mt-1">Try adjusting your filters or add a new student</p>
                                    </div>
                                </td>
                            </tr>
                        <% } else { %>
                            <% students.forEach(student => { %>
                                <tr class="hover:bg-white/5 transition-colors duration-150">
                                    <!-- Name -->
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <div class="flex-shrink-0 h-10 w-10 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-full flex items-center justify-center">
                                                <span class="text-white font-semibold text-sm"><%= student.name.charAt(0).toUpperCase() %></span>
                                            </div>
                                            <div class="ml-4">
                                                <div class="text-sm font-medium text-theme-primary"><%= student.name %></div>
                                            </div>
                                        </div>
                                    </td>

                                    <!-- Branch -->
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-500/20 text-blue-300 border border-blue-500/30">
                                            <%= student.branch %>
                                        </span>
                                    </td>

                                    <!-- Hosteller Status -->
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <% if (student.isHosteller) { %>
                                            <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-500/20 text-green-300 border border-green-500/30">
                                                <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                                                </svg>
                                                Hosteller
                                            </span>
                                        <% } else { %>
                                            <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-500/20 text-gray-300 border border-gray-500/30">
                                                Day Scholar
                                            </span>
                                        <% } %>
                                    </td>

                                    <!-- CGPA -->
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <div class="text-sm font-semibold text-white"><%= student.cgpa %></div>
                                            <div class="ml-2 text-xs text-gray-400">/ 10</div>
                                        </div>
                                        <div class="text-xs text-gray-500 mt-1">
                                            <%= (typeof student.cgpaPercentage !== 'undefined' && student.cgpaPercentage !== null) ? student.cgpaPercentage.toFixed(1) + '%' : 'N/A' %>
                                        </div>
                                    </td>

                                    <!-- Attendance -->
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <div class="flex-1">
                                                <div class="text-sm font-semibold text-white"><%= student.attendance %>%</div>
                                                <div class="w-full bg-gray-700 rounded-full h-1.5 mt-2">
                                                    <div class="bg-gradient-to-r from-indigo-500 to-purple-600 h-1.5 rounded-full" style="width: <%= student.attendance %>%"></div>
                                                </div>
                                            </div>
                                        </div>
                                    </td>

                                    <!-- Overall Percentage -->
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <% 
                                        const overallPct = (typeof student.overallPercentage !== 'undefined' && student.overallPercentage !== null) ? student.overallPercentage : 0;
                                        let badgeClass = 'bg-gray-500/20 text-gray-300 border-gray-500/30';
                                        if (overallPct >= 85) {
                                            badgeClass = 'bg-green-500/20 text-green-300 border-green-500/30';
                                        } else if (overallPct >= 70) {
                                            badgeClass = 'bg-blue-500/20 text-blue-300 border-blue-500/30';
                                        } else if (overallPct >= 60) {
                                            badgeClass = 'bg-yellow-500/20 text-yellow-300 border-yellow-500/30';
                                        } else {
                                            badgeClass = 'bg-red-500/20 text-red-300 border-red-500/30';
                                        }
                                        %>
                                        <span class="px-3 py-1 inline-flex text-sm leading-5 font-bold rounded-full <%= badgeClass %> border">
                                            <%= overallPct.toFixed(2) %>%
                                        </span>
                                    </td>

                                    <!-- Actions -->
                                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                        <div class="flex items-center justify-end space-x-2">
                                            <a href="/students/edit/<%= student._id %>" class="inline-flex items-center px-3 py-2 text-xs font-medium text-yellow-300 bg-yellow-500/10 hover:bg-yellow-500/20 rounded-lg transition-all duration-200 border border-yellow-500/30">
                                                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                                                </svg>
                                                Edit
                                            </a>
                                            <form action="/students/delete/<%= student._id %>" method="POST" class="inline-block" onsubmit="return confirm('Are you sure you want to delete this student?');">
                                                <button type="submit" class="inline-flex items-center px-3 py-2 text-xs font-medium text-red-300 bg-red-500/10 hover:bg-red-500/20 rounded-lg transition-all duration-200 border border-red-500/30">
                                                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                                    </svg>
                                                    Delete
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } %>
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            <div class="px-6 py-4 border-t border-white/10 bg-white/5">
                <div class="flex items-center justify-between">
                    <div class="flex items-center space-x-2 text-sm text-gray-400">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                        <span>Page <span class="font-semibold text-white"><%= currentPage %></span> of <span class="font-semibold text-white"><%= totalPages %></span></span>
                    </div>
                    <div class="flex space-x-2">
                        <% if (currentPage > 1) { %>
                            <a href="?page=<%= currentPage - 1 %>&branch=<%= filters.branch || '' %>&name=<%= filters.name || '' %>&sortBy=<%= sortBy || 'name' %>" class="inline-flex items-center px-4 py-2 text-sm font-medium text-gray-300 bg-white/5 hover:bg-white/10 rounded-lg transition-all duration-200 border border-white/10">
                                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                                </svg>
                                Previous
                            </a>
                        <% } %>
                        <% if (currentPage < totalPages) { %>
                            <a href="?page=<%= currentPage + 1 %>&branch=<%= filters.branch || '' %>&name=<%= filters.name || '' %>&sortBy=<%= sortBy || 'name' %>" class="inline-flex items-center px-4 py-2 text-sm font-medium text-gray-300 bg-white/5 hover:bg-white/10 rounded-lg transition-all duration-200 border border-white/10">
                                Next
                                <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                </svg>
                            </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="mt-12 pb-8">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="border-t border-white/10 pt-8">
                <div class="flex flex-col md:flex-row items-center justify-between gap-4">
                    <p class="text-sm text-gray-400">
                        © 2024 Student Management System. Built for Shipsy Assignment.
                    </p>
                    <div class="flex items-center space-x-6 text-sm text-gray-400">
                        <span class="flex items-center">
                            <svg class="w-4 h-4 mr-2 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                            System Online
                        </span>
                        <span>MongoDB Connected</span>
                        <a href="https://github.com/raiumang07/ai-campus-assignment" 
                           target="_blank" 
                           class="flex items-center hover:text-white transition-colors">
                            <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 24 24">
                                <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"/>
                            </svg>
                            GitHub Repo by Umang Rai
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>